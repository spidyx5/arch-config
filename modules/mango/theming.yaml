# MangoWC window manager
# Custom Wayland compositor configuration

description: MangoWC Theming Packages

packages:
  # Mango-specific packages only

# Conflicts with other window managers
conflicts: []
